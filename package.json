{
  "name": "mentoring10-batch6",
  "version": "1.0.0",
  "description": "MVC + Repositories Design Pattern",
  "main": "index.js",
  "scripts": {
    "test": "jest --detectOpenHandles --runInBand --forceExit --coverage --verbose --watchAll",
    "dev": "nodemon ./bin/www.js",
    "docker": "NODE_ENV=docker && node ./bin/www.js",
    "docker-test": "npx sequelize-cli db:create --env test && npx sequelize-cli db:migrate --env test && jest --detectOpenHandles --runInBand --forceExit --coverage --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "sequelize": "^6.37.1",
    "sequelize-cli": "^6.6.2",
    "supertest": "^6.3.4"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
